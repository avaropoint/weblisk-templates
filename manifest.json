{
  "version": "3.0.0",
  "description": "Official Weblisk project templates — blueprint-driven, no generated code checked in",

  "defaults": {
    "name": "My App",
    "cdn": "https://cdn.weblisk.dev/"
  },

  "templates": {
    "client/starter": {
      "description": "Client website built entirely from blueprints — the default for weblisk new",
      "path": "client/starter/",
      "default": true
    },
    "server/starter": {
      "description": "Minimal Weblisk server — one domain, one agent, one page",
      "path": "server/starter/"
    }
  }
}
